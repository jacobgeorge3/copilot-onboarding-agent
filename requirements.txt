flask==3.1.0
gunicorn==23.0.0

# Data persistence
SQLAlchemy>=2.0.0

# Azure SQL driver — only needed when DATABASE_URL points to an Azure SQL / mssql+pyodbc URL.
# Not required for the default SQLite path. Install separately if switching to Azure SQL:
#   pip install pyodbc>=4.0.39
# pyodbc>=4.0.39
